# 📊 Admin Panel - Architecture & Flow Diagram

## 🏗️ Application Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    ADMIN PANEL                              │
│                  Travel App                                 │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      FRONTEND LAYER                         │
├─────────────────────────────────────────────────────────────┤
│  ✅ Blade Templates                                         │
│  ✅ Bootstrap 5 CSS Framework                              │
│  ✅ Bootstrap Icons                                        │
│  ✅ Responsive Design                                      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                     ROUTING LAYER                           │
├─────────────────────────────────────────────────────────────┤
│  routes/web.php                                            │
│  ├─ /admin/login               (Public)                    │
│  ├─ /admin/dashboard           (Protected)                 │
│  ├─ /admin/users/*             (Protected + Admin)         │
│  ├─ /admin/tours/*             (Protected + Admin)         │
│  └─ /admin/blogs/*             (Protected + Admin)         │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                   MIDDLEWARE LAYER                          │
├─────────────────────────────────────────────────────────────┤
│  ✅ Authenticate                (User login check)          │
│  ✅ IsAdmin                     (Role check: admin)         │
│  ✅ VerifyCsrfToken             (Security)                 │
│  ✅ StartSession                (Session management)        │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                  CONTROLLER LAYER                           │
├─────────────────────────────────────────────────────────────┤
│  Admin/AuthController.php                                  │
│  ├─ showLogin()                                            │
│  ├─ login()                                                │
│  └─ logout()                                               │
│                                                             │
│  Admin/DashboardController.php                             │
│  └─ index()                                                │
│                                                             │
│  Admin/UserController.php (ResourceController)            │
│  ├─ index()    ├─ create()    ├─ store()                  │
│  ├─ show()     ├─ edit()      ├─ update()                 │
│  └─ destroy()                                              │
│                                                             │
│  Admin/TourController.php (ResourceController)            │
│  ├─ index()    ├─ create()    ├─ store()                  │
│  ├─ show()     ├─ edit()      ├─ update()                 │
│  └─ destroy()                                              │
│                                                             │
│  Admin/BlogController.php (ResourceController)            │
│  ├─ index()    ├─ create()    ├─ store()                  │
│  ├─ show()     ├─ edit()      ├─ update()                 │
│  └─ destroy()                                              │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    MODEL LAYER                              │
├─────────────────────────────────────────────────────────────┤
│  ✅ User Model                                             │
│     ├─ id, name, email, password, role                    │
│     └─ Eloquent ORM                                       │
│                                                             │
│  ✅ Tour Model                                             │
│     ├─ id, name, slug, destination, description...        │
│     └─ Eloquent ORM                                       │
│                                                             │
│  ✅ Blog Model                                             │
│     ├─ id, title, slug, content, category...              │
│     └─ Eloquent ORM                                       │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                   DATABASE LAYER                            │
├─────────────────────────────────────────────────────────────┤
│  ✅ SQLite (Development)                                   │
│  ✅ MySQL (Production)                                     │
│                                                             │
│  Tables:                                                    │
│  ├─ users                                                  │
│  ├─ tours                                                  │
│  ├─ blogs                                                  │
│  ├─ migrations                                             │
│  └─ sessions                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 User Authentication Flow

```
┌──────────────────┐
│   User Access    │
│  /admin/login    │
└────────┬─────────┘
         │
         ↓
┌──────────────────────────────┐
│  Check if already logged in  │
└────────┬────────────┬────────┘
         │            │
      YES│            │NO
         ↓            ↓
    Redirect      Show Login
    Dashboard     Form
         ↑            │
         │            ↓
         │    User enters
         │    Email & Password
         │            │
         │            ↓
         │    Find user in DB
         │            │
         │     ┌──────┴──────┐
         │     │             │
         │     ↓             ↓
         │   NOT FOUND    FOUND
         │     │             │
         │     │             ↓
         │     │        Verify password
         │     │             │
         │     │        ┌────┴────┐
         │     │        │          │
         │     │        ↓          ↓
         │     │       WRONG      CORRECT
         │     │        │          │
         │     └────┬───┘          ↓
         │          │        Check role
         │          ↓             │
         │     Show Error    ┌────┴───┐
         │        Message    │        │
         │          ▲        ↓        ↓
         │          │      ADMIN    USER
         │          │        │        │
         │          │        ↓        ↓
         │          │     LOGIN    DENIED
         │          │        │        │
         │          │        ↓        ↓
         └──────────┤    Create   Show Error
                    │    Session    Message
                    │        │
                    ↓        ↓
              Set Cookie  Redirect
              (auth)       to Login
                    │
                    ↓
              Redirect to
              Dashboard
```

---

## 📱 Page Flow & Navigation

```
┌───────────────┐
│  LOGIN PAGE   │
└───────┬───────┘
        │
        ↓ (auth success)
┌────────────────────────────────────┐
│        DASHBOARD                   │
│  ├─ Stats (Users, Tours, Blogs)   │
│  ├─ Recent Users                   │
│  ├─ Recent Tours                   │
│  └─ Recent Blogs                   │
└────────┬───────────────────────────┘
         │
    ┌────┼────┬───────────┐
    ↓    ↓    ↓           ↓
┌──────┐┌────┐┌───────┐┌──────┐
│USERS ││TOUR││BLOGS  ││LOGOUT│
└──┬───┘└─┬──┘└───┬───┘└──────┘
   │      │       │
   ↓      ↓       ↓
┌────────┐┌──────┐┌──────┐
│ INDEX  ││INDEX ││INDEX │
│ CREATE ││CREATE││CREATE│
│ SHOW   ││SHOW  ││SHOW  │
│ EDIT   ││EDIT  ││EDIT  │
│ DELETE ││DELETE││DELETE│
└────────┘└──────┘└──────┘
```

---

## 🔐 Authorization Flow

```
┌──────────────────────┐
│  Incoming Request    │
│ (e.g., GET /admin)   │
└──────────┬───────────┘
           │
           ↓
┌──────────────────────────────┐
│   Check Authenticate         │
│   (Session exists?)          │
└──────────┬──────────┬────────┘
           │          │
        YES│          │NO
           ↓          ↓
      Continue   Redirect to
                 /admin/login
           │
           ↓ (if NO route group)
┌──────────────────────────────┐
│   Check IsAdmin Middleware   │
│   (role == 'admin'?)         │
└──────────┬──────────┬────────┘
           │          │
        YES│          │NO
           ↓          ↓
      Execute    Redirect with
      Controller Error Message
           │
           ↓
      Return View/
      Response
```

---

## 📊 CRUD Operations Flow

### Create (Store)
```
GET /resource/create
     ↓
Show form
     ↓
POST /resource
     ↓
Validate data
     ↓
Store in DB
     ↓
Flash success message
     ↓
Redirect to index
```

### Read (Show)
```
GET /resource/{id}
     ↓
Find model
     ↓
Check exists?
     ├─ No → 404
     └─ Yes → Show view
```

### Update (Edit)
```
GET /resource/{id}/edit
     ↓
Show form with data
     ↓
PUT /resource/{id}
     ↓
Validate data
     ↓
Update in DB
     ↓
Flash success message
     ↓
Redirect to show page
```

### Delete (Destroy)
```
DELETE /resource/{id}
     ↓
Find model
     ↓
Confirm delete
     ↓
Delete from DB
     ↓
Flash success message
     ↓
Redirect to index
```

---

## 🗂️ File Organization

```
app/
├── Http/
│   ├── Controllers/
│   │   └── Admin/
│   │       ├── AuthController.php
│   │       ├── DashboardController.php
│   │       ├── UserController.php
│   │       ├── TourController.php
│   │       └── BlogController.php
│   ├── Middleware/
│   │   ├── Authenticate.php
│   │   ├── IsAdmin.php
│   │   └── ...
│   └── Kernel.php
│
├── Models/
│   ├── User.php
│   ├── Tour.php
│   └── Blog.php
│
└── ...

resources/
├── views/
│   ├── admin/
│   │   ├── layouts/
│   │   │   └── app.blade.php
│   │   ├── auth/
│   │   │   └── login.blade.php
│   │   ├── dashboard.blade.php
│   │   ├── users/
│   │   ├── tours/
│   │   └── blogs/
│   └── ...
└── ...

routes/
├── web.php
├── api.php
└── ...

database/
├── migrations/
│   └── 2025_11_12_*.php
├── seeders/
│   └── AdminSeeder.php
└── ...
```

---

## 🔄 Request-Response Cycle

```
┌─────────────────────────────────┐
│     HTTP Request from Client    │
│  (Browser, API, etc.)           │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│  1. Route Matching              │
│     (routes/web.php)            │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│  2. Middleware Processing       │
│     (Before)                    │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│  3. Call Controller Action      │
│     (Process business logic)    │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│  4. Query Database (if needed)  │
│     (Eloquent ORM)              │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│  5. Render View / Return Data   │
│     (Blade template)            │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│  6. Middleware Processing       │
│     (After)                     │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│  7. Send HTTP Response          │
│     (HTML, JSON, etc.)          │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│     Browser Displays Response   │
└─────────────────────────────────┘
```

---

## 🎯 Data Relationships

```
Users
├── Role: user | admin
├── Status: active | inactive
└── Can manage: tours, blogs, users (if admin)

Tours
├── Name, destination, price
├── Duration, image, rating
└── Related to: nothing (standalone)

Blogs
├── Title, content, category
├── Author, tags, published_at
└── Related to: nothing (standalone)
```

---

## 💾 Database Schema

```
USERS TABLE
┌──────────────────┐
│ id (PK)          │ ← Primary Key
│ name             │
│ email (UNIQUE)   │ ← Unique
│ password         │ ← Hashed
│ role             │ ← user | admin
│ email_verified_at│
│ remember_token   │
│ created_at       │
│ updated_at       │
└──────────────────┘

TOURS TABLE
┌──────────────────┐
│ id (PK)          │
│ name             │
│ slug (UNIQUE)    │
│ destination      │
│ description      │
│ price            │
│ duration         │
│ image            │
│ rating           │
│ created_at       │
│ updated_at       │
└──────────────────┘

BLOGS TABLE
┌──────────────────┐
│ id (PK)          │
│ title            │
│ slug (UNIQUE)    │
│ excerpt          │
│ content          │
│ author (JSON)    │
│ category         │
│ tags (JSON)      │
│ image            │
│ published_at     │
│ read_time        │
│ views            │
│ likes            │
│ created_at       │
│ updated_at       │
└──────────────────┘
```

---

**✨ Architecture is clean, scalable, and follows Laravel best practices!**
